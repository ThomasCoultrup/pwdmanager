import tkinter as tk
from tkinter import filedialog, Text
import os

window = tk.Tk()
window.title("Password Manager")


canvas = tk.Canvas(window, height=700, width=700, bg="#000000")
greeting = tk.Label(text="Welcome to Password Manager")
greeting.pack()
canvas.pack()


username = tk.Text(window, height=1, width=30)
username.pack()
userfield = "Username"
username.insert(tk.END, userfield)

password = tk.Text(window, height=1, width=30)
password.pack()
pwdfield = "Password"
password.insert(tk.END, pwdfield)

#frame = tk.Frame(window, bg="white")
#frame.place(relwidth=0.10, relheight=0.10, relx=0.5, rely=0.5)

openFile = tk.Button(window, text="Open FIle", padx=10, pady=5, fg="black", bg="#ffffff")

openFile.pack()


window.mainloop()
